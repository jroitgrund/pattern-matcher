{
  "name": "pattern-matcher",
  "version": "0.0.6",
  "description": "Simple pattern matching",
  "main": "build/src/index.js",
  "repository": "https://github.com/jroitgrund/pattern-matcher",
  "author": "Jonathan Roitgrund <jroitgrund@gmail.com>",
  "license": "MIT",
  "scripts": {
    "test": "tslint -p tsconfig.json && tsc && mocha build/test --recursive --reporter mocha-junit-reporter",
    "test:local": "tslint -p tsconfig.json && tsc && mocha build/test --recursive"
  },
  "devDependencies": {
    "@types/chai": "^3.5.0",
    "@types/mocha": "^2.2.40",
    "@types/source-map-support": "^0.2.28",
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "mocha-junit-reporter": "^1.13.0",
    "source-map-support": "^0.4.14",
    "tsc": "^1.20150623.0",
    "tslint": "^5.1.0",
    "typescript": "^2.2.2"
  }
}
